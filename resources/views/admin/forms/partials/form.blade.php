<label class="grid gap-1 text-sm"><span>Periode</span><select name="evaluation_period_id" class="rounded-md border-zinc-300" required>@foreach ($periods as $period)<option value="{{ $period->id }}" @selected((int) old('evaluation_period_id', $form?->evaluation_period_id) === $period->id)>{{ $period->name }}</option>@endforeach</select><x-form-error name="evaluation_period_id" /></label>
<label class="grid gap-1 text-sm"><span>Judul</span><input name="title" value="{{ old('title', $form?->title) }}" class="rounded-md border-zinc-300" required><x-form-error name="title" /></label>
<label class="grid gap-1 text-sm"><span>Deskripsi</span><textarea name="description" class="rounded-md border-zinc-300">{{ old('description', $form?->description) }}</textarea><x-form-error name="description" /></label>
<label class="grid gap-1 text-sm"><span>Target</span><select name="target_type" class="rounded-md border-zinc-300" required>@foreach (['layanan_akademik','pembelajaran','fasilitas','administrasi','kepuasan_umum'] as $target)<option value="{{ $target }}" @selected(old('target_type', $form?->target_type) === $target)>{{ $target }}</option>@endforeach</select><x-form-error name="target_type" /></label>
<input type="hidden" name="is_active" value="0"><label class="flex gap-2 text-sm"><input name="is_active" type="checkbox" value="1" @checked(old('is_active', $form?->is_active ?? true))> Aktif</label>
