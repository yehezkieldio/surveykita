<x-layouts.admin title="Mahasiswa - SurveyKita" heading="Data Mahasiswa"><div class="sk-pagehead"><p class="sk-pagehead-copy">Kelola profil akademik dan akun mahasiswa yang dapat mengisi evaluasi.</p><x-button :href="route('admin.students.create')">Tambah Mahasiswa</x-button></div><x-table :headers="['NIM', 'Nama & Email', 'Program Studi', 'Kelas', 'Respons', 'Aksi']">@forelse ($students as $student)<tr class="hover:bg-zinc-50"><td class="px-5 py-4 font-mono text-sm text-zinc-700">{{ $student->nim }}</td><td class="px-5 py-4"><span class="font-semibold">{{ $student->name }}</span><span class="mt-1 block text-sm text-zinc-500">{{ $student->user->email }}</span></td><td class="px-5 py-4 text-zinc-600">{{ $student->study_program }}</td><td class="px-5 py-4 text-zinc-600">{{ $student->class_name }}</td><td class="px-5 py-4 font-mono text-sm text-zinc-700">{{ $student->responses_count }}</td><td class="px-5 py-4"><div class="sk-link-row"><a class="sk-link" href="{{ route('admin.students.show', $student) }}">Detail</a><a class="sk-link" href="{{ route('admin.students.edit', $student) }}">Edit</a><form method="POST" action="{{ route('admin.students.destroy', $student) }}" onsubmit="return confirm('Apakah Anda yakin ingin menghapus mahasiswa ini?')">@csrf @method('DELETE')<button class="sk-danger-link" type="submit">Hapus</button></form></div></td></tr>@empty<tr><td class="px-5 py-10" colspan="6"><x-empty-state title="Belum ada data mahasiswa" description="Data profil mahasiswa Universitas Mulia belum dimasukkan ke dalam sistem." /></td></tr>@endforelse</x-table><x-pagination :paginator="$students" /></x-layouts.admin>
