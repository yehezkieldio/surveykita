<label class="grid gap-1 text-sm"><span>Form</span><select name="evaluation_form_id" class="rounded-md border-zinc-300" required>@foreach ($forms as $form)<option value="{{ $form->id }}" @selected((int) old('evaluation_form_id', $question?->evaluation_form_id) === $form->id)>{{ $form->title }}</option>@endforeach</select><x-form-error name="evaluation_form_id" /></label>
<label class="grid gap-1 text-sm"><span>Kategori</span><select name="question_category_id" class="rounded-md border-zinc-300" required>@foreach ($categories as $category)<option value="{{ $category->id }}" @selected((int) old('question_category_id', $question?->question_category_id) === $category->id)>{{ $category->name }}</option>@endforeach</select><x-form-error name="question_category_id" /></label>
<label class="grid gap-1 text-sm"><span>Pertanyaan</span><textarea name="question_text" class="rounded-md border-zinc-300" required>{{ old('question_text', $question?->question_text) }}</textarea><x-form-error name="question_text" /></label>
<label class="grid gap-1 text-sm"><span>Urutan</span><input name="sort_order" type="number" min="0" value="{{ old('sort_order', $question?->sort_order ?? 0) }}" class="rounded-md border-zinc-300" required><x-form-error name="sort_order" /></label>
<input type="hidden" name="is_required" value="0"><label class="flex gap-2 text-sm"><input name="is_required" type="checkbox" value="1" @checked(old('is_required', $question?->is_required ?? true))> Wajib dijawab</label>
