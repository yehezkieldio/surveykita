<x-layouts.student title="Evaluasi Aktif - SurveyKita" heading="Evaluasi Aktif" eyebrow="Daftar Kuesioner Akademik"><div class="grid gap-6 md:grid-cols-2">@forelse ($forms as $form)<x-card heading="{{ $form->title }}" subheading="Periode: {{ $form->evaluationPeriod->name }}. Kategori: {{ ucwords(str_replace('_', ' ', $form->target_type)) }}"><p class="text-sm leading-6 text-zinc-600">{{ $form->description ?: 'Evaluasi kepuasan layanan akademik Universitas Mulia.' }}</p><div class="mt-6 flex flex-wrap items-center gap-2">@if ($form->submitted)<x-badge variant="success">Sudah Dikirim</x-badge><x-button variant="secondary" :href="route('student.evaluations.show', $form)">Lihat Detail</x-button>@else<x-button :href="route('student.evaluations.fill', $form)">Mulai Isi</x-button><x-button variant="secondary" :href="route('student.evaluations.show', $form)">Detail</x-button>@endif</div></x-card>@empty<div class="md:col-span-2"><x-empty-state title="Belum ada kuesioner aktif" description="Form evaluasi yang aktif pada periode berjalan akan tampil di halaman ini." /></div>@endforelse</div></x-layouts.student>
